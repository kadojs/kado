mixin alerts
  each alert in flash('error')
    .alert.alert-danger= alert
  each alert in flash('warning')
    .alert.alert-warning= alert
  each alert in flash('notice')
    .alert.alert-info= alert
  each alert in flash('success')
    .alert.alert-success= alert